@using Test.Data.Enum
@using Test.record
@model PLdata

<link href="@Url.Content("~/css/postlist.css")" rel="stylesheet"/>
<script src="@Url.Content("~/js/model.js")"></script>
<script type="module">

    const row = new Row(
        document.querySelector('main.main'), 
        "@Model.userID"
    );
    await row.get("@Model.url");
    row.generate_cards();

    // await row.update();
    // new Worker();

</script>